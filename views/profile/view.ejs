<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
</head>
<body>
    <%- include('../partials/header.ejs') %>

<h1><%= user.username %>'s Profile</h1>
<p><strong>Email:</strong> <%= user.email %></p>
<p><strong>About:</strong> <%= user.bio %></p>
<p><strong>Social Links:</strong> <%= user.social_links %></p>
<% if (user.role === 'dj') { %>
<p><strong>Experience:</strong> <%= user.experience %></p>
       <p><strong>Check Out My Mixes:</strong></p>
<ul>
  <% if (user.sampleMixes && user.sampleMixes.length > 0) { %>
    <% user.sampleMixes.forEach(function(mix) { %>
      <li><a href="<%= mix %>" target="_blank">Clean Party Vibes</a></li>
    <% }) %>
  <% } else { %>
    <li>No mixes available</li>
  <% } %>
</ul>     
<% } %>



<%- include('../partials/footer.ejs') %>

</body>
</html>